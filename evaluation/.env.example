# Services under evaluation (already running). Override if not on localhost defaults.
RETRIEVAL_BASE_URL=http://localhost:8200
BE_BASE_URL=http://localhost:5000

# HTTP timeout per request, in seconds.
EVAL_REQUEST_TIMEOUT_SECONDS=30
