# Pinned to the versions verified working locally. The embedding stack
# (transformers/tokenizers/sentence-transformers/huggingface-hub) MUST stay in
# lockstep: mismatched releases make the tokenizer reject batched inputs with
# "TextEncodeInput must be Union[TextInputSequence, Tuple[...]]" at reindex time.
# Leaving these unpinned let the image resolve newer, incompatible versions than
# local, which is why reindex worked locally but 500'd in the cluster.

# Web server
fastapi==0.139.0
uvicorn[standard]==0.51.0

# Elasticsearch + async HTTP
elasticsearch==8.19.3
aiohttp==3.14.1

# Config
python-dotenv==1.2.2

# Embedding stack (keep these versions in lockstep)
langchain-core==1.4.9
langchain-huggingface==1.2.2
sentence-transformers==5.6.0
transformers==5.14.0
tokenizers==0.22.2
huggingface-hub==1.23.0
safetensors==0.8.0
numpy==2.5.1
