{{- if and .Values.rag.enabled .Values.rag.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "backend.fullname" . }}-rag
  labels:
    component: rag
    {{- include "backend.labels" . | nindent 4 }}
  annotations:
    {{- toYaml .Values.rag.ingress.annotations | nindent 4 }}
spec:
  ingressClassName: {{ .Values.rag.ingress.className }}
  {{- if .Values.rag.ingress.tls }}
  tls:
    {{- range .Values.rag.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.rag.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "backend.fullname" $ }}-rag
                port:
                  number: {{ $.Values.rag.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
