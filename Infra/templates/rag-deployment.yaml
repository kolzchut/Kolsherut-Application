{{- if .Values.rag.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "backend.fullname" . }}-rag
  labels:
    component: rag
    {{- include "backend.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.rag.replicaCount }}
  {{- with .Values.rag.strategy }}
  strategy:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  selector:
    matchLabels:
      component: rag
      {{- include "backend.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        component: rag
        {{- include "backend.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      initContainers:
        - name: wait-for-elasticsearch
          image: busybox:1.28
          command:
            - 'sh'
            - '-c'
            - |
              until nc -z -v -w30 {{ include "backend.fullname" . }}-elasticsearch 9200; do
                sleep 2
              done
      containers:
        - name: {{ .Chart.Name }}-rag
          image: "{{ .Values.rag.image.repository }}:{{ .Values.rag.image.tag }}"
          imagePullPolicy: {{ .Values.rag.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.rag.service.port }}
              protocol: TCP

          # Startup gate: /health only answers once the models are warmed, so this
          # holds traffic (and liveness) off until the heavy model load completes.
          startupProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: {{ .Values.rag.probes.startup.periodSeconds }}
            failureThreshold: {{ .Values.rag.probes.startup.failureThreshold }}
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: {{ .Values.rag.probes.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.rag.probes.liveness.periodSeconds }}
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: {{ .Values.rag.probes.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.rag.probes.readiness.periodSeconds }}

          resources:
            {{- toYaml .Values.rag.resources | nindent 12 }}
          env:
            - name: ELASTIC_URL
              value: "http://{{ include "backend.fullname" . }}-elasticsearch:9200"
          envFrom:
            - configMapRef:
                name: {{ include "backend.fullname" . }}-rag-config
            - secretRef:
                name: {{ include "backend.fullname" . }}-secrets
{{- end }}
