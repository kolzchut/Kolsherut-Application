{{- if and .Values.retrieval.enabled .Values.retrieval.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "backend.fullname" . }}-retrieval
  labels:
    component: retrieval
    {{- include "backend.labels" . | nindent 4 }}
  annotations:
    {{- toYaml .Values.retrieval.ingress.annotations | nindent 4 }}
spec:
  ingressClassName: {{ .Values.retrieval.ingress.className }}
  {{- if .Values.retrieval.ingress.tls }}
  tls:
    {{- range .Values.retrieval.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.retrieval.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "backend.fullname" $ }}-retrieval
                port:
                  number: {{ $.Values.retrieval.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
