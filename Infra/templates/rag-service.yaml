{{- if .Values.rag.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "backend.fullname" . }}-rag
  labels:
    component: rag
    {{- include "backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.rag.service.type }}
  ports:
    - port: {{ .Values.rag.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    component: rag
    {{- include "backend.selectorLabels" . | nindent 4 }}
{{- end }}
